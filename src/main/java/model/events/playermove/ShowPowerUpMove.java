package model.events.playermove;

import model.player_package.PlayerColor;
import view.View;

public class ShowPowerUpMove extends PlayerMove {
    public ShowPowerUpMove(View view){
        super(view);
    }
}
